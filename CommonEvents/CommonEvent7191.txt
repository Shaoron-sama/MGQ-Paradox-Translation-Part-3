CommonEvent 7191
Name = "オーラン：会話"

  999([7191])
  ConditionalBranch([1, 820, 0, 0, 0])
    ControlVariables([820, 820, 0, 0, 1])
    Empty([])

  BranchEnd([])
  PlayBGM(["RPG::BGM(@name=\"ero2\", @volume=75, @pitch=100)"])
  ConditionalBranch([1, 1143, 0, 10, 2])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["【オーラン】"])
    ShowText(["俺とバトルファックだと……？"])
    ShowText(["面白いな、挑んでくるヤツは久しぶりだぜ。"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([1, 1143, 0, 11, 1])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["【オーラン】"])
    ShowText(["俺とバトルファックだと……？"])
    ShowText(["世界がメチャクチャだってのに、お前は元気だな。"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([1, 820, 0, 2, 1])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["【オーラン】"])
    ShowText(["賭け金は350G、俺が負けても何もやらん。"])
    ShowText(["お前が前に持っていったからな……"])
    Empty([])

  Else([])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["【オーラン】"])
    ShowText(["賭け金は350G、俺が負けたらアクセサリをやろう。"])
    ShowText(["武技を使用する際、SP消費を軽減する値打ちモノだ。"])
    Empty([])

  BranchEnd([])
  ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
  ShowText(["【オーラン】"])
  ShowText(["言っとくが、俺もアサシン一族の出だ。"])
  ShowText(["性技にも、それなりに心得はあるぜ……？"])
  ShowChoices([["バトルファックを挑む", "やめておく"], 2])
  When([0, "バトルファックを挑む"])
    ConditionalBranch([7, 350, 0])
      ConditionalBranch([0, 2331, 1])
        ChangePlayerFollowers([1])
        ShowTextAttributes(["", 0, 0, 2])
        ShowText(["仲間達は、建物から出て行った……"])
        Empty([])

      BranchEnd([])
      CallCommonEvent([7192])
      ExitEventProcessing([])
      Empty([])

    Else([])
      ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
      ShowText(["【オーラン】"])
      ShowText(["おいおい、金がないじゃないか。"])
      ShowText(["冷やかしも大概にするんだな……"])
      ConditionalBranch([0, 443, 0])
        ExitEventProcessing([])
        Empty([])

      BranchEnd([])
      ConditionalBranch([0, 2331, 1])
        ErasePicture([5])
        PlayBGM(["RPG::BGM(@name=\"city1\", @volume=100, @pitch=100)"])
        Empty([])

      BranchEnd([])
      ExitEventProcessing([])
      Empty([])

    BranchEnd([])
    Empty([])

  When([1, "やめておく"])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["【オーラン】"])
    ShowText(["そこで退くのか……"])
    ShowText(["せっかく搾り取ってやろうと思ったんだがな。"])
    ConditionalBranch([0, 443, 0])
      ExitEventProcessing([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([0, 2331, 1])
      ErasePicture([5])
      PlayBGM(["RPG::BGM(@name=\"city1\", @volume=100, @pitch=100)"])
      Empty([])

    BranchEnd([])
    ExitEventProcessing([])
    Empty([])

  ChoicesEnd([])
  Empty([])
